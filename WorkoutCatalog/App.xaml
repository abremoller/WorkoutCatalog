<Application x:Class="WorkoutCatalog.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WorkoutCatalog.ViewModels"
             xmlns:views="clr-namespace:WorkoutCatalog.Views">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- ViewModel to View mappings -->
            <DataTemplate DataType="{x:Type vm:ExerciseListViewModel}">
                <views:ExerciseListView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ExerciseDetailViewModel}">
                <views:ExerciseDetailView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:TopTenViewModel}">
                <views:TopTenView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:HistoryViewModel}">
                <views:HistoryView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:PlaylistViewModel}">
                <views:PlaylistView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
