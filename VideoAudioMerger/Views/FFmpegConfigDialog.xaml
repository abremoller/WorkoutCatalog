<Window x:Class="VideoAudioMerger.Views.FFmpegConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FFmpeg Configuration" Height="250" Width="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="FFmpeg Path Not Configured" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,20">
            Please specify the path to ffmpeg.exe. This is required to process video and audio files.
        </TextBlock>

        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PathTextBox" Grid.Column="0" 
                     VerticalAlignment="Center" Padding="5"/>
            <Button Grid.Column="1" Content="Browse..." 
                    Click="Browse_Click"
                    Padding="15,5" Margin="10,0,0,0"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OK_Click" 
                    Padding="20,5" Margin="0,0,10,0"
                    Background="#2196F3" Foreground="White"/>
            <Button Content="Cancel" Click="Cancel_Click" 
                    Padding="20,5"/>
        </StackPanel>
    </Grid>
</Window>
